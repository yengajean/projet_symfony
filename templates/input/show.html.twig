{% extends 'base.html.twig' %}

{% block title %}Input{% endblock %}

{% block body %}
    <h1>Input</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ input.id }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ input.qty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_input_index') }}">back to list</a>

    <a href="{{ path('app_input_edit', {'id': input.id}) }}">edit</a>

    {{ include('input/_delete_form.html.twig') }}
{% endblock %}
