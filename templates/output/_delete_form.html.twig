<form method="post" action="{{ path('app_output_delete', {'id': output.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ output.id) }}">
    <button class="btn">Delete</button>
</form>
