{% extends 'base.html.twig' %}

{% block title %}Output{% endblock %}

{% block body %}
    <h1>Output</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ output.id }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ output.qty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_output_index') }}">back to list</a>

    <a href="{{ path('app_output_edit', {'id': output.id}) }}">edit</a>

    {{ include('output/_delete_form.html.twig') }}
{% endblock %}
